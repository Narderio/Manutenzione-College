package org.elis.manutenzione;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Classe principale dell'applicazione Spring Boot per la gestione delle manutenzioni.
 */
@SpringBootApplication
public class ManutenzioneApplication {

    /**
     * Metodo main per avviare l'applicazione Spring Boot.
     *
     * @param args Argomenti della riga di comando.
     */
    public static void main(String[] args) {
        SpringApplication.run(ManutenzioneApplication.class, args);
    }


}
